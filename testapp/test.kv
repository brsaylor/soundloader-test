#:kivy 1.10.1

<MainWindow>:
    BoxLayout:
        orientation: 'vertical'
        width: root.width

        TextInput:
            text: root.filename
            on_text: root.filename = self.text
        Button:
            text: "Play"
            on_press: root.play_sound()
